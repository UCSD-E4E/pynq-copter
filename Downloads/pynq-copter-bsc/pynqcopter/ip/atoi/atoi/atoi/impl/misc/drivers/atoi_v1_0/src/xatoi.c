// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

/***************************** Include Files *********************************/
#include "xatoi.h"

/************************** Function Implementation *************************/
#ifndef __linux__
int XAtoi_CfgInitialize(XAtoi *InstancePtr, XAtoi_Config *ConfigPtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(ConfigPtr != NULL);

    InstancePtr->Axilites_BaseAddress = ConfigPtr->Axilites_BaseAddress;
    InstancePtr->IsReady = XIL_COMPONENT_IS_READY;

    return XST_SUCCESS;
}
#endif

void XAtoi_Start(XAtoi *InstancePtr) {
    u32 Data;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XAtoi_ReadReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_AP_CTRL) & 0x80;
    XAtoi_WriteReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_AP_CTRL, Data | 0x01);
}

u32 XAtoi_IsDone(XAtoi *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XAtoi_ReadReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_AP_CTRL);
    return (Data >> 1) & 0x1;
}

u32 XAtoi_IsIdle(XAtoi *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XAtoi_ReadReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_AP_CTRL);
    return (Data >> 2) & 0x1;
}

u32 XAtoi_IsReady(XAtoi *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XAtoi_ReadReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_AP_CTRL);
    // check ap_start to see if the pcore is ready for next input
    return !(Data & 0x1);
}

void XAtoi_EnableAutoRestart(XAtoi *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XAtoi_WriteReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_AP_CTRL, 0x80);
}

void XAtoi_DisableAutoRestart(XAtoi *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XAtoi_WriteReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_AP_CTRL, 0);
}

u32 XAtoi_Get_in_r_BaseAddress(XAtoi *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XATOI_AXILITES_ADDR_IN_R_BASE);
}

u32 XAtoi_Get_in_r_HighAddress(XAtoi *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XATOI_AXILITES_ADDR_IN_R_HIGH);
}

u32 XAtoi_Get_in_r_TotalBytes(XAtoi *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XATOI_AXILITES_ADDR_IN_R_HIGH - XATOI_AXILITES_ADDR_IN_R_BASE + 1);
}

u32 XAtoi_Get_in_r_BitWidth(XAtoi *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XATOI_AXILITES_WIDTH_IN_R;
}

u32 XAtoi_Get_in_r_Depth(XAtoi *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XATOI_AXILITES_DEPTH_IN_R;
}

u32 XAtoi_Write_in_r_Words(XAtoi *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XATOI_AXILITES_ADDR_IN_R_HIGH - XATOI_AXILITES_ADDR_IN_R_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Axilites_BaseAddress + XATOI_AXILITES_ADDR_IN_R_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XAtoi_Read_in_r_Words(XAtoi *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XATOI_AXILITES_ADDR_IN_R_HIGH - XATOI_AXILITES_ADDR_IN_R_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Axilites_BaseAddress + XATOI_AXILITES_ADDR_IN_R_BASE + (offset + i)*4);
    }
    return length;
}

u32 XAtoi_Write_in_r_Bytes(XAtoi *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XATOI_AXILITES_ADDR_IN_R_HIGH - XATOI_AXILITES_ADDR_IN_R_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Axilites_BaseAddress + XATOI_AXILITES_ADDR_IN_R_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XAtoi_Read_in_r_Bytes(XAtoi *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XATOI_AXILITES_ADDR_IN_R_HIGH - XATOI_AXILITES_ADDR_IN_R_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Axilites_BaseAddress + XATOI_AXILITES_ADDR_IN_R_BASE + offset + i);
    }
    return length;
}

void XAtoi_InterruptGlobalEnable(XAtoi *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XAtoi_WriteReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_GIE, 1);
}

void XAtoi_InterruptGlobalDisable(XAtoi *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XAtoi_WriteReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_GIE, 0);
}

void XAtoi_InterruptEnable(XAtoi *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XAtoi_ReadReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_IER);
    XAtoi_WriteReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_IER, Register | Mask);
}

void XAtoi_InterruptDisable(XAtoi *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XAtoi_ReadReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_IER);
    XAtoi_WriteReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_IER, Register & (~Mask));
}

void XAtoi_InterruptClear(XAtoi *InstancePtr, u32 Mask) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XAtoi_WriteReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_ISR, Mask);
}

u32 XAtoi_InterruptGetEnabled(XAtoi *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XAtoi_ReadReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_IER);
}

u32 XAtoi_InterruptGetStatus(XAtoi *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XAtoi_ReadReg(InstancePtr->Axilites_BaseAddress, XATOI_AXILITES_ADDR_ISR);
}

