// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// ==============================================================

#define AP_INT_MAX_W 32678

#include <systemc>
#include <iostream>
#include <cstdlib>
#include <cstddef>
#include <stdint.h>
#include "SysCFileHandler.h"
#include "ap_int.h"
#include "ap_fixed.h"
#include <complex>
#include <stdbool.h>
#include "autopilot_cbe.h"
#include "ap_stream.h"
#include "hls_stream.h"
#include "hls_half.h"
#include "hls_signal_handler.h"

using namespace std;
using namespace sc_core;
using namespace sc_dt;


// [dump_struct_tree [build_nameSpaceTree] dumpedStructList] ---------->


// [dump_enumeration [get_enumeration_list]] ---------->


// wrapc file define: "regs_in_V"
#define AUTOTB_TVIN_regs_in_V  "../tv/cdatafile/c.mixer.autotvin_regs_in_V.dat"
// wrapc file define: "m_V"
#define AUTOTB_TVOUT_m_V  "../tv/cdatafile/c.mixer.autotvout_m_V.dat"
#define AUTOTB_TVIN_m_V  "../tv/cdatafile/c.mixer.autotvin_m_V.dat"

#define INTER_TCL  "../tv/cdatafile/ref.tcl"

// tvout file define: "m_V"
#define AUTOTB_TVOUT_PC_m_V  "../tv/rtldatafile/rtl.mixer.autotvout_m_V.dat"

class INTER_TCL_FILE {
	public:
		INTER_TCL_FILE(const char* name) {
			mName = name;
			regs_in_V_depth = 0;
			m_V_depth = 0;
			trans_num =0;
		}

		~INTER_TCL_FILE() {
			mFile.open(mName);
			if (!mFile.good()) {
				cout << "Failed to open file ref.tcl" << endl;
				exit (1);
			}
			string total_list = get_depth_list();
			mFile << "set depth_list {\n";
			mFile << total_list;
			mFile << "}\n";
			mFile << "set trans_num "<<trans_num<<endl;
			mFile.close();
		}

		string get_depth_list () {
			stringstream total_list;
			total_list << "{regs_in_V " << regs_in_V_depth << "}\n";
			total_list << "{m_V " << m_V_depth << "}\n";
			return total_list.str();
		}

		void set_num (int num , int* class_num) {
			(*class_num) = (*class_num) > num ? (*class_num) : num;
		}
	public:
		int regs_in_V_depth;
		int m_V_depth;
		int trans_num;

	private:
		ofstream mFile;
		const char* mName;
};

extern void mixer (
ap_fixed<16, 1, (ap_q_mode) 5, (ap_o_mode)3, 0> regs_in[4],
ap_fixed<16, 1, (ap_q_mode) 5, (ap_o_mode)3, 0> m[6]);

void AESL_WRAP_mixer (
ap_fixed<16, 1, (ap_q_mode) 5, (ap_o_mode)3, 0> regs_in[4],
ap_fixed<16, 1, (ap_q_mode) 5, (ap_o_mode)3, 0> m[6])
{
	refine_signal_handler();
	fstream wrapc_switch_file_token;
	wrapc_switch_file_token.open(".hls_cosim_wrapc_switch.log");
	int AESL_i;
	if (wrapc_switch_file_token.good())
	{
		CodeState = ENTER_WRAPC_PC;
		static unsigned AESL_transaction_pc = 0;
		string AESL_token;
		string AESL_num;
		static AESL_FILE_HANDLER aesl_fh;


		// output port post check: "m_V"
		aesl_fh.read(AUTOTB_TVOUT_PC_m_V, AESL_token); // [[transaction]]
		if (AESL_token != "[[transaction]]")
		{
			exit(1);
		}
		aesl_fh.read(AUTOTB_TVOUT_PC_m_V, AESL_num); // transaction number

		if (atoi(AESL_num.c_str()) == AESL_transaction_pc)
		{
			aesl_fh.read(AUTOTB_TVOUT_PC_m_V, AESL_token); // data

			sc_bv<16> *m_V_pc_buffer = new sc_bv<16>[6];
			int i = 0;

			while (AESL_token != "[[/transaction]]")
			{
				bool no_x = false;
				bool err = false;

				// search and replace 'X' with "0" from the 1st char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('X');
					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'm_V', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				no_x = false;

				// search and replace 'x' with "0" from the 3rd char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('x', 2);

					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'm_V', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				// push token into output port buffer
				if (AESL_token != "")
				{
					m_V_pc_buffer[i] = AESL_token.c_str();
					i++;
				}

				aesl_fh.read(AUTOTB_TVOUT_PC_m_V, AESL_token); // data or [[/transaction]]

				if (AESL_token == "[[[/runtime]]]" || aesl_fh.eof(AUTOTB_TVOUT_PC_m_V))
				{
					exit(1);
				}
			}

			// ***********************************
			if (i > 0)
			{
				// RTL Name: m_V
				{
					// bitslice(15, 0)
					// {
						// celement: m.V(15, 0)
						// {
							sc_lv<16>* m_V_lv0_0_5_1 = new sc_lv<16>[6];
						// }
					// }

					// bitslice(15, 0)
					{
						int hls_map_index = 0;
						// celement: m.V(15, 0)
						{
							// carray: (0) => (5) @ (1)
							for (int i_0 = 0; i_0 <= 5; i_0 += 1)
							{
								if (&(m[0]) != NULL) // check the null address if the c port is array or others
								{
									m_V_lv0_0_5_1[hls_map_index].range(15, 0) = sc_bv<16>(m_V_pc_buffer[hls_map_index].range(15, 0));
									hls_map_index++;
								}
							}
						}
					}

					// bitslice(15, 0)
					{
						int hls_map_index = 0;
						// celement: m.V(15, 0)
						{
							// carray: (0) => (5) @ (1)
							for (int i_0 = 0; i_0 <= 5; i_0 += 1)
							{
								// sub                    : i_0
								// ori_name               : m[i_0]
								// sub_1st_elem           : 0
								// ori_name_1st_elem      : m[0]
								// output_left_conversion : (m[i_0]).range()
								// output_type_conversion : (m_V_lv0_0_5_1[hls_map_index]).to_string(SC_BIN).c_str()
								if (&(m[0]) != NULL) // check the null address if the c port is array or others
								{
									(m[i_0]).range() = (m_V_lv0_0_5_1[hls_map_index]).to_string(SC_BIN).c_str();
									hls_map_index++;
								}
							}
						}
					}
				}
			}

			// release memory allocation
			delete [] m_V_pc_buffer;
		}

		AESL_transaction_pc++;
	}
	else
	{
		CodeState = ENTER_WRAPC;
		static unsigned AESL_transaction;

		static AESL_FILE_HANDLER aesl_fh;

		// "regs_in_V"
		char* tvin_regs_in_V = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_regs_in_V);

		// "m_V"
		char* tvin_m_V = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_m_V);
		char* tvout_m_V = new char[50];
		aesl_fh.touch(AUTOTB_TVOUT_m_V);

		CodeState = DUMP_INPUTS;
		static INTER_TCL_FILE tcl_file(INTER_TCL);
		int leading_zero;

		// [[transaction]]
		sprintf(tvin_regs_in_V, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_regs_in_V, tvin_regs_in_V);

		sc_bv<16>* regs_in_V_tvin_wrapc_buffer = new sc_bv<16>[4];

		// RTL Name: regs_in_V
		{
			// bitslice(15, 0)
			{
				int hls_map_index = 0;
				// celement: regs_in.V(15, 0)
				{
					// carray: (0) => (3) @ (1)
					for (int i_0 = 0; i_0 <= 3; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : regs_in[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : regs_in[0]
						// regulate_c_name       : regs_in_V
						// input_type_conversion : (regs_in[i_0]).range().to_string(SC_BIN).c_str()
						if (&(regs_in[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<16> regs_in_V_tmp_mem;
							regs_in_V_tmp_mem = (regs_in[i_0]).range().to_string(SC_BIN).c_str();
							regs_in_V_tvin_wrapc_buffer[hls_map_index].range(15, 0) = regs_in_V_tmp_mem.range(15, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 4; i++)
		{
			sprintf(tvin_regs_in_V, "%s\n", (regs_in_V_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_regs_in_V, tvin_regs_in_V);
		}

		tcl_file.set_num(4, &tcl_file.regs_in_V_depth);
		sprintf(tvin_regs_in_V, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_regs_in_V, tvin_regs_in_V);

		// release memory allocation
		delete [] regs_in_V_tvin_wrapc_buffer;

		// [[transaction]]
		sprintf(tvin_m_V, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_m_V, tvin_m_V);

		sc_bv<16>* m_V_tvin_wrapc_buffer = new sc_bv<16>[6];

		// RTL Name: m_V
		{
			// bitslice(15, 0)
			{
				int hls_map_index = 0;
				// celement: m.V(15, 0)
				{
					// carray: (0) => (5) @ (1)
					for (int i_0 = 0; i_0 <= 5; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : m[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : m[0]
						// regulate_c_name       : m_V
						// input_type_conversion : (m[i_0]).range().to_string(SC_BIN).c_str()
						if (&(m[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<16> m_V_tmp_mem;
							m_V_tmp_mem = (m[i_0]).range().to_string(SC_BIN).c_str();
							m_V_tvin_wrapc_buffer[hls_map_index].range(15, 0) = m_V_tmp_mem.range(15, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 6; i++)
		{
			sprintf(tvin_m_V, "%s\n", (m_V_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_m_V, tvin_m_V);
		}

		tcl_file.set_num(6, &tcl_file.m_V_depth);
		sprintf(tvin_m_V, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_m_V, tvin_m_V);

		// release memory allocation
		delete [] m_V_tvin_wrapc_buffer;

// [call_c_dut] ---------->

		CodeState = CALL_C_DUT;
		mixer(regs_in, m);

		CodeState = DUMP_OUTPUTS;

		// [[transaction]]
		sprintf(tvout_m_V, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVOUT_m_V, tvout_m_V);

		sc_bv<16>* m_V_tvout_wrapc_buffer = new sc_bv<16>[6];

		// RTL Name: m_V
		{
			// bitslice(15, 0)
			{
				int hls_map_index = 0;
				// celement: m.V(15, 0)
				{
					// carray: (0) => (5) @ (1)
					for (int i_0 = 0; i_0 <= 5; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : m[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : m[0]
						// regulate_c_name       : m_V
						// input_type_conversion : (m[i_0]).range().to_string(SC_BIN).c_str()
						if (&(m[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<16> m_V_tmp_mem;
							m_V_tmp_mem = (m[i_0]).range().to_string(SC_BIN).c_str();
							m_V_tvout_wrapc_buffer[hls_map_index].range(15, 0) = m_V_tmp_mem.range(15, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 6; i++)
		{
			sprintf(tvout_m_V, "%s\n", (m_V_tvout_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVOUT_m_V, tvout_m_V);
		}

		tcl_file.set_num(6, &tcl_file.m_V_depth);
		sprintf(tvout_m_V, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVOUT_m_V, tvout_m_V);

		// release memory allocation
		delete [] m_V_tvout_wrapc_buffer;

		CodeState = DELETE_CHAR_BUFFERS;
		// release memory allocation: "regs_in_V"
		delete [] tvin_regs_in_V;
		// release memory allocation: "m_V"
		delete [] tvout_m_V;
		delete [] tvin_m_V;

		AESL_transaction++;

		tcl_file.set_num(AESL_transaction , &tcl_file.trans_num);
	}
}

