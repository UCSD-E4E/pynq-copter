// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

/***************************** Include Files *********************************/
#include "xmixer.h"

/************************** Function Implementation *************************/
#ifndef __linux__
int XMixer_CfgInitialize(XMixer *InstancePtr, XMixer_Config *ConfigPtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(ConfigPtr != NULL);

    InstancePtr->Axilites_BaseAddress = ConfigPtr->Axilites_BaseAddress;
    InstancePtr->IsReady = XIL_COMPONENT_IS_READY;

    return XST_SUCCESS;
}
#endif

void XMixer_Start(XMixer *InstancePtr) {
    u32 Data;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XMixer_ReadReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_AP_CTRL) & 0x80;
    XMixer_WriteReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_AP_CTRL, Data | 0x01);
}

u32 XMixer_IsDone(XMixer *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XMixer_ReadReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_AP_CTRL);
    return (Data >> 1) & 0x1;
}

u32 XMixer_IsIdle(XMixer *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XMixer_ReadReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_AP_CTRL);
    return (Data >> 2) & 0x1;
}

u32 XMixer_IsReady(XMixer *InstancePtr) {
    u32 Data;

    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Data = XMixer_ReadReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_AP_CTRL);
    // check ap_start to see if the pcore is ready for next input
    return !(Data & 0x1);
}

void XMixer_EnableAutoRestart(XMixer *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XMixer_WriteReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_AP_CTRL, 0x80);
}

void XMixer_DisableAutoRestart(XMixer *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XMixer_WriteReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_AP_CTRL, 0);
}

u32 XMixer_Get_regs_in_V_BaseAddress(XMixer *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XMIXER_AXILITES_ADDR_REGS_IN_V_BASE);
}

u32 XMixer_Get_regs_in_V_HighAddress(XMixer *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XMIXER_AXILITES_ADDR_REGS_IN_V_HIGH);
}

u32 XMixer_Get_regs_in_V_TotalBytes(XMixer *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XMIXER_AXILITES_ADDR_REGS_IN_V_HIGH - XMIXER_AXILITES_ADDR_REGS_IN_V_BASE + 1);
}

u32 XMixer_Get_regs_in_V_BitWidth(XMixer *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XMIXER_AXILITES_WIDTH_REGS_IN_V;
}

u32 XMixer_Get_regs_in_V_Depth(XMixer *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XMIXER_AXILITES_DEPTH_REGS_IN_V;
}

u32 XMixer_Write_regs_in_V_Words(XMixer *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XMIXER_AXILITES_ADDR_REGS_IN_V_HIGH - XMIXER_AXILITES_ADDR_REGS_IN_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Axilites_BaseAddress + XMIXER_AXILITES_ADDR_REGS_IN_V_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XMixer_Read_regs_in_V_Words(XMixer *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XMIXER_AXILITES_ADDR_REGS_IN_V_HIGH - XMIXER_AXILITES_ADDR_REGS_IN_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Axilites_BaseAddress + XMIXER_AXILITES_ADDR_REGS_IN_V_BASE + (offset + i)*4);
    }
    return length;
}

u32 XMixer_Write_regs_in_V_Bytes(XMixer *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XMIXER_AXILITES_ADDR_REGS_IN_V_HIGH - XMIXER_AXILITES_ADDR_REGS_IN_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Axilites_BaseAddress + XMIXER_AXILITES_ADDR_REGS_IN_V_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XMixer_Read_regs_in_V_Bytes(XMixer *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XMIXER_AXILITES_ADDR_REGS_IN_V_HIGH - XMIXER_AXILITES_ADDR_REGS_IN_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Axilites_BaseAddress + XMIXER_AXILITES_ADDR_REGS_IN_V_BASE + offset + i);
    }
    return length;
}

void XMixer_InterruptGlobalEnable(XMixer *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XMixer_WriteReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_GIE, 1);
}

void XMixer_InterruptGlobalDisable(XMixer *InstancePtr) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XMixer_WriteReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_GIE, 0);
}

void XMixer_InterruptEnable(XMixer *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XMixer_ReadReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_IER);
    XMixer_WriteReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_IER, Register | Mask);
}

void XMixer_InterruptDisable(XMixer *InstancePtr, u32 Mask) {
    u32 Register;

    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    Register =  XMixer_ReadReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_IER);
    XMixer_WriteReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_IER, Register & (~Mask));
}

void XMixer_InterruptClear(XMixer *InstancePtr, u32 Mask) {
    Xil_AssertVoid(InstancePtr != NULL);
    Xil_AssertVoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    XMixer_WriteReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_ISR, Mask);
}

u32 XMixer_InterruptGetEnabled(XMixer *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XMixer_ReadReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_IER);
}

u32 XMixer_InterruptGetStatus(XMixer *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XMixer_ReadReg(InstancePtr->Axilites_BaseAddress, XMIXER_AXILITES_ADDR_ISR);
}

