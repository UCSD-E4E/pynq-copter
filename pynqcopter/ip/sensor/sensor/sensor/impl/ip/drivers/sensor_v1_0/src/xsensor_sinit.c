// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xsensor.h"

extern XSensor_Config XSensor_ConfigTable[];

XSensor_Config *XSensor_LookupConfig(u16 DeviceId) {
	XSensor_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XSENSOR_NUM_INSTANCES; Index++) {
		if (XSensor_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XSensor_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XSensor_Initialize(XSensor *InstancePtr, u16 DeviceId) {
	XSensor_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XSensor_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XSensor_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

