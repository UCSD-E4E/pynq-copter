// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xrpy_to_duty.h"

extern XRpy_to_duty_Config XRpy_to_duty_ConfigTable[];

XRpy_to_duty_Config *XRpy_to_duty_LookupConfig(u16 DeviceId) {
	XRpy_to_duty_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XRPY_TO_DUTY_NUM_INSTANCES; Index++) {
		if (XRpy_to_duty_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XRpy_to_duty_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XRpy_to_duty_Initialize(XRpy_to_duty *InstancePtr, u16 DeviceId) {
	XRpy_to_duty_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XRpy_to_duty_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XRpy_to_duty_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

